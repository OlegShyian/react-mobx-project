import React from 'react'

const StyledComponent:React.FC = () => {
    return (
        <div>
            <h1>My counter{}</h1>
            <div>
                <button>plus one</button>
                <button>mines one</button>
            </div>
        </div>
    )
}

export default StyledComponent
